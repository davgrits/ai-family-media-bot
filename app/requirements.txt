# Runtime dependencies (Docker pip path). Mirrors pyproject.toml [project].dependencies.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
prometheus-client>=0.20,<1.0
httpx>=0.27,<1.0
opentelemetry-sdk>=1.24,<2.0
opentelemetry-exporter-otlp-proto-http>=1.24,<2.0
# AWS SDK — Bedrock (story + image), SQS, S3 adapters.
boto3>=1.34,<2.0
# CRT extra: required by the `aws login` credential provider on dev machines.
botocore[crt]>=1.34,<2.0
# GCP SDKs — Pub/Sub queue, GCS media storage, and Vertex AI via the Gen AI SDK.
google-cloud-pubsub>=2.21,<3.0
google-cloud-storage>=2.16,<4.0
google-genai>=1.0,<2.0
