apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "family-media-bot.name" . }}-web
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "family-media-bot.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  replicas: {{ .Values.web.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "family-media-bot.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: web
  template:
    metadata:
      annotations:
        {{- include "family-media-bot.configChecksums" . | nindent 8 }}
      labels:
        {{- include "family-media-bot.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/component: web
    spec:
      serviceAccountName: {{ include "family-media-bot.name" . }}
      volumes:
        {{- include "family-media-bot.registryVolume" . | nindent 8 }}
      containers:
        - name: web
          image: "{{ required "image.repository is required" .Values.image.repository }}:{{ required "image.tag is required" .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: {{ include "family-media-bot.name" . }}-config
          volumeMounts:
            {{- include "family-media-bot.registryMount" . | nindent 12 }}
          env:
            - name: RUN_MODE
              value: web
            - name: TELEGRAM_POLLING
              value: "true"
            {{- include "family-media-bot.registryEnv" . | nindent 12 }}
            - name: TELEGRAM_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.app.telegramSecretName }}
                  key: TELEGRAM_BOT_TOKEN
          livenessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /readyz, port: http }
            initialDelaySeconds: 5
            periodSeconds: 15
          resources:
            {{- toYaml .Values.web.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
