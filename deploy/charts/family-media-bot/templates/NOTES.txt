Release {{ .Release.Name }} is installed in namespace {{ .Release.Namespace }}.

Check the warm web tier:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/component=web

Check the warm worker. The replica count is fixed rather than scaling from zero:
activation on Pub/Sub backlog measured ~4m43s, because the backlog metric
samples on a ~60s interval and can gap for minutes.
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/component=worker

Follow application logs:
  kubectl -n {{ .Release.Namespace }} logs deploy/{{ include "family-media-bot.name" . }}-web -f
  kubectl -n {{ .Release.Namespace }} logs deploy/{{ include "family-media-bot.name" . }}-worker -f
