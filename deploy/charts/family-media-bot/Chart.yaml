apiVersion: v2
name: family-media-bot
description: GKE deployment for the AI family media bot
type: application
# 0.3.0 is a breaking values change: the provider selector, the autoscaler
# replica envelope, nodeSelectors, and tolerations are all gone.
version: 0.3.0
appVersion: "0.1.0"
